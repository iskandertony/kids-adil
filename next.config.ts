/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/kids-adil',
    assetPrefix: '/kids-adil/',
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
